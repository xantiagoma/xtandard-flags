/**
 * Subpath entry for `@xtandard/flags/storage/postgres`. Re-exports the Postgres
 * adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/postgres.ts";
