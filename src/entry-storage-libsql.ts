/**
 * Subpath entry for `@xtandard/flags/storage/libsql`. Re-exports the libSQL/Turso
 * adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/libsql.ts";
