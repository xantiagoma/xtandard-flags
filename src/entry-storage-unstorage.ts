/**
 * Subpath entry for `@xtandard/flags/storage/unstorage`. Re-exports the
 * unstorage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/unstorage.ts";
